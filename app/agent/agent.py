"""The autonomous agent.

Receives a user message and the available tools, lets the LLM decide which
tool (if any) to call, executes it, feeds the result back to the LLM and
returns the final answer.

Implemented in Subtask 8.
"""
