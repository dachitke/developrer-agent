[project]
name = "developer-agent"
version = "0.1.0"
description = "Autonomous Developer Assistant AI Agent built with LangChain"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
